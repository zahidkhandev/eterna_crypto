# --- Docker / Podman Configuration ---
# Use 'docker.io/' for Docker Hub or your private registry
IMAGE_REGISTRY=docker.io/
# 'missing' = pull only if not present (good for local dev/podman)
# 'always' = always pull latest
PULL_POLICY=missing

# --- Database Config ---
DB_USER=user
DB_PASSWORD=password
DB_NAME=dex_engine
DB_PORT=5432
# Prisma Connection String
DATABASE_URL="postgresql://user:password@localhost:5432/dex_engine?schema=public"

# --- Redis Config ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=dex123

# --- App Config ---
NODE_ENV=development
API_PORT=3000
